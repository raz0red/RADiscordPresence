# Build environment for RADiscordPresence.
# golang:1.23-bookworm includes Go 1.23 and all cross-compilation targets.
# All builds use CGO_ENABLED=0 — no C dependencies required.
FROM golang:1.23-bookworm

WORKDIR /src
